schema_version: "1.0"
id: faster-rcnn-resnet50
name: Faster R-CNN ResNet50
version: 1.0.0
description: 从旧训练项目迁移的可独立部署目标检测算法
task_type: object_detection
runtime:
  framework: pytorch
  device: gpu
  min_memory_mb: 4096
input:
  media_types: [image/jpeg, image/png]
output:
  type: object_detection
parameters:
  confidence:
    type: number
    default: 0.5
    minimum: 0
    maximum: 1
